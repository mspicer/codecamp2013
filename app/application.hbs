<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="panel"> 
        <div class="panel-body"> 
        {{#link-to 'index'}}<h1>Codecamp2013</h1>{{/link-to}} 
        </div> 
      </div>
    </div>
    <div class="col-lg-12"> 
      <ul class="nav nav-pills">
        {{#each}}
          {{partial link}}
        {{/each}}
      </ul> 
    </div>
    <div class="col-lg-12">
      {{outlet}}
    </div>
  </div>
</div>
